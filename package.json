{
  "name": "trust-legal-group",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Trust Legal Group — статическая вёрстка (HTML + CSS) под сборку Vite и импорт в Figma (html.to.design).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
